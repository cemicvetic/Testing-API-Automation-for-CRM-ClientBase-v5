import {expect} from 'chai'
describe('Example test', () => {
    it('Check if sum is correct', () => {
        expect(5 + 2).to.eq(7)
    })
    it('Check if find reminder is correct', () => {
        expect(4 % 2).to.eq(0)
    })
})